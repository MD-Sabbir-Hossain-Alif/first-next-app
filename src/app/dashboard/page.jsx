import React from "react";

export const metadata = {
    title: "Dashboard",
    description: "this is main dashboard of this web site",
};

const Dashboard = () => {
    return (
        <div>
            <h1 className="text-center">this is dash board</h1>
        </div>
    );
};

export default Dashboard;
